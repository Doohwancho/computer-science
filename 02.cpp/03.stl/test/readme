---
Tips on writing tests 

1. write modular test
    - Test each method of Vector (e.g., push_back, resize, clear, size, etc.) independently.
2. write edge cases 
    - Test edge cases like resizing to zero, accessing out-of-bounds indices, and adding/removing many elements

