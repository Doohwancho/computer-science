#include <iostream> //input, output에 관련된 파일
//<stdio.h>의 업그레이드 버젼이라고 생각하면 쉬움
#include <string>

using namespace std;

//namespace a
//{
//	int n;
//}
//
//namespace b
//{
//	int n;
//}

int main()
{
	/*
	//예제1
	cout << "Hello, World!" << 10 << 'k' << endl;
	//std : name space (이름 성)
	//cout 데이터를 분리할 때 << 이 기호로 분리. 쉼표의 역할
	//endl : 줄바꿈

	//예제2
	//a::n = 10;
	//b::n = 20; 
	//2개의 n은 성이 다르기 때문에 별개의 n으로 인식됨.

	//예제3
	int a, b;
	cin >> a >> b;//cin : 입력을 받음(형은 자동으로 알아들음)

	cout << a << "+" << b << "=" << a + b << endl;
	
	.//예제4 - string
	string str; //배열형태가 아니니 길이제한이 없음. 또한 string끼리 붙이기 용이
	str = "Hello";
	cout << str << endl;
	

	//예제5 - string
	string name;

	cout << "이름 입력: ";
	cin >> name;

	string message = "안녕하세요," + name + "님";
	cout << message << endl;
	

	//예제6 - 변수의 초기화와 선언

	int a(10); // cpp스타일의 int a = 10;
	int b(a);

	cout << "a = " << a << endl;
	cout << "b = " << b << endl;

	//예제7 - 범위기반 for문

	int arr[10] = { 3,1,4,1,5,9,2,6,5,3 };

	for (int &n : arr) //int n -> call by value / int &n -> call by reference
	{
		cout << n << ' ';
		n++;
	}
	cout << endl;

	for (int n : arr)
	{
		cout << n << ' ';
	}
	cout << endl;
	*/
	
}

